const salaJs = ["Evaldo", "Camis", "Mari"];

const salaPython = ["Jadson", "Danielle"];

// CONCATENA DUAS OU MAIS ARRAYS
const salasUnificadas = salaJs.concat(salaPython);

console.log(salasUnificadas)
